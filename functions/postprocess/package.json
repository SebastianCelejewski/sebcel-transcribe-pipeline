{
  "name": "sebcel-transcribe-postprocess",
  "version": "1.0.0",
  "description": "Post-processing Lambda for AWS Transcribe outputs (TXT + SRT + translations)",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.500.0",
    "@aws-sdk/client-translate": "^3.500.0"
  },
   "scripts": {
    "test": "vitest"
  }
}
